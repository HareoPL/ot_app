#include "unity_fixture.h"


TEST_GROUP(ot_app_coap_uri_obs);

TEST_SETUP(ot_app_coap_uri_obs)
{
    /* Init before every test */
}

TEST_TEAR_DOWN(ot_app_coap_uri_obs)
{
    /* Cleanup after every test */
}

TEST(ot_app_coap_uri_obs, test1)
{
    TEST_FAIL_MESSAGE("1: It is template for unit test. Use it to create your own test.");
}

